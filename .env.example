# Usado apenas pelo script scripts/fetch-google-reviews.mjs (busca local, opcional).
# Em produção essas variáveis vivem como Secrets do GitHub Actions, nunca aqui.
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=
